se Intent Scraper

A Python tool to scrape and analyze Twitter conversations regarding Electric Vehicle buying decisions.

## Setup
1. Clone the repo: `git clone <url>`
2. Install requirements: `pip install -r requirements.txt`
3. Add your credentials to `config.yaml`.
4. Run: `python src/scraper.py`
